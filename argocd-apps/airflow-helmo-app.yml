papiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airflow-helm
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/Lucasnobrepro/airflow.git'
    targetRevision: main
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: airflow
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true